var test1=(x,y)=>
        {
            return Math.min(x,y)
        }

var test2=(x,y)=>
        {
            return Math.max(x,y)
        }


        var a = prompt("entre a : ")
        var b = prompt("entre b : ")

        console.log(test1(a,b))
        console.log(test2(a,b))
